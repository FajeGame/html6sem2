spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
  cache:
    type: redis

logging:
  level:
    root: WARN
    com.example.lab3: DEBUG

jwt:
  secret: test-jwt-secret-key-for-integration-tests-32b
  expiration-ms: 86400000
